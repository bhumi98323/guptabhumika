<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome to this site!</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/3.7.0/animate.min.css">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
      <link rel="stylesheet" href="../stylesheets/style.css">
   <style>
        body, html {
            margin: 0;
            padding: 0;
            height: 100%;
        }
        .overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            background-color: rgb(251, 255, 251); 
        }
        .blur {
    filter: blur(4px); 
}
        .bhumika {
            font-size: 8rem;
            text-align: center;
            letter-spacing: -4px;
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            color: transparent; 
            white-space: nowrap; 
            overflow: hidden; 
            animation: colorFill 2s forwards, moveLeft 2s 2s forwards; 
        }

        @keyframes colorFill {
            0% {
               scale: 2;
                width: 0; 
            }
            100% {
                color: #4da5df; 
                width: 30%; /
            }
        }

        @keyframes moveLeft {
            0% {
                transform: translate(-50%, -50%); 
            }
            100% {
                transform: translate(-50%, -50%); 
            }
        }
     
        .move {
    position: relative;
    animation: moveLeftToRight 4s ease-in-out infinite;
  }

  @keyframes moveLeftToRight {
    0% {
      left: 0;
      transform: translateX(0);
    }
    50% {
      transform: translateX(50px);
    }
    100% {
      left: calc(100% - 100px);
      transform: translateX(0);
    }
  }
</style>
   
        
    </head>
<body>
    <div class="overlay">
        <img class="image w-33 h-35 overflow-hidden" src="https://cdn.dribbble.com/users/1278301/screenshots/4342353/face.gif" alt="">   
        <h1 id="hello" class="absolute  text-black font-bold top-50" style="font-size: 15rem; font-family: Voyage;">Hello</h1>
      <div class="page1 text-white  absolute w-full h-full bg-black opacity-0" style="top: 100%;">
        <div class="bhumika font-style: italic  text-white">Bhumika Gupta</div>
        <div class="navs font-bold w-full absolute top-2.5 right-0 bg-lime-600 pt-8 pr-6">
            <div class="nav text-white flex justify-end">
                <h3 class="mr-5">Home</h3>
                <h3 class="mr-5">About</h3>
                <h3 class="mr-5">Project</h3>
                <h3 class="mr-5">Skills</h3>
                <h3>Contact</h3>
            </div>
        </div>
        <div class="flex justify-center items-center h-screen data-scroll data-scroll-speed= 2">
            <h2 class="create text-white uppercase text-center transition-opacity duration-100 opacity-30 move"  style="font-size: 9rem;" >Creative Developer </h2>
        </div>
        <div class="flex justify-center items-center overflow-hidden">
            <img class="bimage absolute min-h-full " src="../images/thebhumi-removebg-preview.png" alt="">
          </div>
       </div> 
       <div class="page2 w-full h-full text-white text-9xl  absolute" style="top: 100%; background-color: #181a1e; font-family :Helvetica; cursor: pointer;">
        <h2 class="px-4" style="font-family: fantasy;">I Am</h2>
        <h2 class="Exp px-4 left-28" style="font-family: fantasy;">Experienced</h2>


        <p class="t1 text-2xl absolute right-0 top-32 mr-5 pr-6 hidden">I have crafted 20+ website across<br/> various platform and industries</p>
        <h2 class="pas px-4" style="font-family: fantasy;">Passionate</h2>
        <p class="t2 text-2xl absolute right-0 m-6 pr-6 hidden">I put my heart and soul into my work<br/> and always wiling to go extra mile to <br/> deliver the best result</p>
        <h2 class="cre px-4" style="font-family: fantasy;">Creative</h2>
        <p class="t3 text-2xl absolute right-0  top-1/2 m-6 pr-6 hidden">I Love to create interactive<br/> and immersive experience using <br/> GSAP , LOCOMOTIVE , SCROLLTRIGGER AND MORE</p>
        <h2 class="dev px-4" style="font-family:fantasy; font-weight: 300;">Developer</h2>
        <p class="t4 text-2xl absolute right-0  top-3/4 m-6 pr-6 hidden">I am Looking of new challenge<br/> to develop my Skills </p>
    </div>
    
    
    </div>
    
    
    <script src="https://cdn.jsdelivr.net/npm/locomotive-scroll@4.1.4/dist/locomotive-scroll.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/scrolltrigger@1.0.1/index.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/lettering.js/0.7.0/jquery.lettering.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/textillate/0.4.0/jquery.textillate.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.9.1/gsap.min.js"></script>
<script> 
function init() {
  gsap.registerPlugin(ScrollTrigger);

  // Using Locomotive Scroll from Locomotive https://github.com/locomotivemtl/locomotive-scroll

  const locoScroll = new LocomotiveScroll({
      el: document.querySelector(".overlay"),
      smooth: true
  });
  // each time Locomotive Scroll updates, tell ScrollTrigger to update too (sync positioning)
  locoScroll.on("scroll", ScrollTrigger.update);

  // tell ScrollTrigger to use these proxy methods for the "#main" element since Locomotive Scroll is hijacking things
  ScrollTrigger.scrollerProxy("#main", {
      scrollTop(value) {
          return arguments.length ? locoScroll.scrollTo(value, 0, 0) : locoScroll.scroll.instance.scroll.y;
      }, // we don't have to define a scrollLeft because we're only scrolling vertically.
      getBoundingClientRect() {
          return {
              top: 0,
              left: 0,
              width: window.innerWidth,
              height: window.innerHeight
          };
      },
      // LocomotiveScroll handles things completely differently on mobile devices - it doesn't even transform the container at all! So to get the correct behavior and avoid jitters, we should pin things with position: fixed on mobile. We sense it by checking to see if there's a transform applied to the container (the LocomotiveScroll-controlled element).
      pinType: document.querySelector(".overlay").style.transform ? "transform" : "fixed"
  });



  // each time the window updates, we should refresh ScrollTrigger and then update LocomotiveScroll. 
  ScrollTrigger.addEventListener("refresh", () => locoScroll.update());

  // after everything is set up, refresh() ScrollTrigger and update LocomotiveScroll because padding may have been added for pinning, etc.
  ScrollTrigger.refresh();




}
var Exp= document.querySelectorAll('.Exp');
var pas= document.querySelectorAll('.pas');
var cre= document.querySelectorAll('.cre');
var dev= document.querySelectorAll('.dev');
var t1 = document.querySelector('.t1'); 
var t2 = document.querySelector('.t2'); 
var t3 = document.querySelector('.t3'); 
var t4 = document.querySelector('.t4'); 
Exp.forEach(function(elem) {
    elem.addEventListener("mouseenter", function() {
        t1.style.display = "block";
    });
    elem.addEventListener("mouseleave", function() {
        t1.style.display = "none";
        pas.forEach(function(item) {
            item.classList.remove('blur');
        });
        cre.forEach(function(item) {
            item.classList.remove('blur');
        });
        dev.forEach(function(item) {
            item.classList.remove('blur');
        });
    });
    elem.addEventListener("click", function() {
        pas.forEach(function(item) {
            item.classList.add('blur');
        });
        cre.forEach(function(item) {
            item.classList.add('blur');
        });
        dev.forEach(function(item) {
            item.classList.add('blur');
        });
        elem.classList.remove('blur');
        t1.style.display = "block";
    });
});


pas.forEach(function(elem) {
    elem.addEventListener("mouseenter", function() {
        t2.style.display = "block";
    });
    elem.addEventListener("mouseleave", function(elem){
        t2.style.display = "none";
    })
    pas.forEach(function(elem) {
    elem.addEventListener("mouseenter", function() {
        t2.style.display = "block";
    });
    elem.addEventListener("mouseleave", function() {
        t2.style.display = "none";
        pas.forEach(function(item) {
            item.classList.remove('blur');
        });
        cre.forEach(function(item) {
            item.classList.remove('blur');
        });
        dev.forEach(function(item) {
            item.classList.remove('blur');
        });
    });
    elem.addEventListener("click", function() {
        Exp.forEach(function(item) {
            item.classList.add('blur');
        });
        cre.forEach(function(item) {
            item.classList.add('blur');
        });
        dev.forEach(function(item) {
            item.classList.add('blur');
        });
        elem.classList.remove('blur');
        t2.style.display = "block";
    });
});

});
cre.forEach(function(elem) {
    elem.addEventListener("mouseenter", function() {
        t3.style.display = "block";
    });
    elem.addEventListener("mouseleave", function(elem){
        t3.style.display = "none";
    })
    cre.forEach(function(elem) {
    elem.addEventListener("mouseenter", function() {
        t3.style.display = "block";
    });
    elem.addEventListener("mouseleave", function() {
        t3.style.display = "none";
        pas.forEach(function(item) {
            item.classList.remove('blur');
        });
        Exp.forEach(function(item) {
            item.classList.remove('blur');
        });
        dev.forEach(function(item) {
            item.classList.remove('blur');
        });
    });
    elem.addEventListener("click", function() {
        pas.forEach(function(item) {
            item.classList.add('blur');
        });
        cre.forEach(function(item) {
            item.classList.add('blur');
        });
        dev.forEach(function(item) {
            item.classList.add('blur');
        });
        elem.classList.remove('blur');
        t1.style.display = "block";
    });
});

});

dev.forEach(function(elem) {
    elem.addEventListener("mouseenter", function() {
        t4.style.display = "block";
    });
    elem.addEventListener("mouseleave", function(elem){
        t4.style.display = "none";
    })
    dev.forEach(function(elem) {
    elem.addEventListener("mouseenter", function() {
        t1.style.display = "block";
    });
    elem.addEventListener("mouseleave", function() {
        t4.style.display = "none";
        pas.forEach(function(item) {
            item.classList.remove('blur');
        });
        cre.forEach(function(item) {
            item.classList.remove('blur');
        });
        Exp.forEach(function(item) {
            item.classList.remove('blur');
        });
    });
    elem.addEventListener("click", function() {
        pas.forEach(function(item) {
            item.classList.add('blur');
        });
        cre.forEach(function(item) {
            item.classList.add('blur');
        });
        Exp.forEach(function(item) {
            item.classList.add('blur');
        });
        elem.classList.remove('blur');
        t4.style.display = "block";
    });
});

});

        $('#hello').textillate({ in: { effect: 'bounceOut' } });
        
        gsap.to("#hello",{
            y: -90,
            duration: 2,
            delay: 1,
            opacity: 0,
        });

        gsap.from(".image",{
            y: -80,
            duration: 1,
            delay: 3,
            scale: 5,
            opacity: 0,
        });

        gsap.to(".page1",{
            top: "0%",
            delay: 4,
            opacity: 1,
            ease: "expo.out"
        });
        gsap.to(".bhumika", {
            top: "10%",
            left: "10%",
            fontSize: "2rem",
            duration: 4,
            scale: 2,
            delay: 4, 
            opacity: 1,
            ease: "expo.out"
        });
        
        
        gsap.to('.bimage', {
            top: "10%", 
            opacity: 1, 
            duration: 2, 
            delay: 3
        });




    </script>
    
</body>
</html>
